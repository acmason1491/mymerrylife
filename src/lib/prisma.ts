// Prisma is no longer used at runtime — the database schema is managed via
// Supabase migrations (supabase/migrations/00001_init.sql).
// This file exists only for build compatibility.
export const prisma = {} as Record<string, unknown>;
